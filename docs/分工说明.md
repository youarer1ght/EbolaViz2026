# 分工说明

> 数据可视化导论课程大作业 | 2026 Spring | 3 人团队

## 成员分工

### A — 数据 + 文档

| 模块 | 具体工作 |
|------|---------|
| 数据采集 | 从 INRB-UMIE/Ebola_DRC_2026 仓库获取 INSP 逐日疫情数据、WorldPop 人口数据、healthsites.io 医疗机构数据、FAO LCCS 城乡分类数据、CCVI 社会经济数据 |
| 数据预处理 | 编写 `scripts/build_real_data.py`（CSV→JSON 构建脚本，含日期格式转换、省名标准化、跨文件一致性校验） |
| 数据维护 | 维护 `data/cases.csv`、`data/demographics.csv`、`data/policy_events.csv`、`data/border_poe.csv` 四个 CSV 源文件；编写 `scripts/merge_geojson.py` 合并 DRC+UGA GeoJSON |
| 中期文档 | 撰写 `docs/中期文档.md` 全部七章（绪论、技术基础、数据集、总体设计、详细设计与实现、结果与讨论、总结展望） |
| 系统文档 | 撰写 `docs/系统文档.md`（设计需求、案例展示、AI 使用声明） |

### B — 可视化 + 交互核心

| 模块 | 具体工作 |
|------|---------|
| 状态管理 | 编写 `js/store.js`（~81 行手写观察者模式 Store + reducer 纯函数，含 _resetId 机制）、`js/actions.js`（7 个 action creator） |
| 数据工具 | 编写 `js/utils/dataLoader.js`（loadAllData、filterCases、summarizeByRegion、summarizeByProvince、stateKeysEqual 等工具函数，含 memoization 缓存优化）、`js/utils/colors.js`（配色方案） |
| 视图 ① 热力图 | 编写 `js/views/heatmapView.js`（~738 行，单 Series choropleth + 拖拽抑制 hover 防卡顿、分栏布局：左侧 ADM1 choropleth + 右侧省级放大 GeoJSON + 卫生区真实坐标散点选择、roam 缩放、zoom 状态保持、全选/清除按钮） |
| 视图 ③ 平行坐标 | 编写 `js/views/parallelView.js`（~291 行，5 轴多因素关联分析，按省份着色 + 死亡率不透明度，brush 多轴框选 + click-to-select + DOM 清除按钮，stateKeysEqual 跳过无关渲染） |
| 视图 ⑤ 详情面板 | 编写 `js/views/detailView.js`（~239 行，HTML 统计卡片 + ECharts 水平条形图，条形图按确诊数排名、CFR 颜色编码、点击选中，去重 filterCases 计算） |
| 单元测试 | 编写 `tests/unit.test.js`（95 断言：Store reducer 纯度、Actions 结构、filterCases 过滤/memoization、数据聚合、颜色算法、stateKeysEqual） |
| 冒烟测试 | 编写 `tests/smoke.test.js`（Node.js 自动化验证全部 5 视图 init + destroy，mock ECharts + document，加载真实 JSON 数据） |
| 数据测试 | 编写 `tests/data.test.js`（674 断言：JSON 字段验证、跨文件交叉校验、GeoJSON 结构、日期范围检查、政策事件校验） |
| 合约测试 | 编写 `tests/option.test.js`（78 断言：5 视图 init→render→reset→destroy 全生命周期，9 种状态转换） |
| 联动测试 | 编写 `tests/coordination.test.js`（33 断言：dataZoom→SET_TIME_RANGE、click→SET_SELECTED_REGIONS、RESET_ALL 清除） |

### C — 前端 + 部署

| 模块 | 具体工作 |
|------|---------|
| 页面骨架 | 编写 `index.html`（SPA 入口、6 容器 DOM 含热力图分栏、ECharts CDN、标题栏播放/暂停/重置控件） |
| 布局样式 | 编写 `css/style.css`（CSS Grid 五视图布局 + 热力图分栏 flexbox + 响应式断点 + 政策类型配色） |
| 应用入口 | 编写 `js/main.js`（loadAllData → createStore → init 5 views → playback control + Space/R 键盘快捷键） |
| 视图 ② 时序图 | 编写 `js/views/timelineView.js`（多系列折线 + dataZoom 刷选 → SET_TIME_RANGE，dataZoom 为唯一时间过滤器） |
| 视图 ④ 政策轴 | 编写 `js/views/policyView.js`（5 类型散点标记 + 病例背景趋势 + 点击 → SET_SELECTED_POLICY_IDS） |
| 部署运维 | GitHub Pages 部署配置、Git 分支管理、提交规范（conventional commits）、代码审查 |

## 协作机制

- 代码仓库：GitHub youarer1ght/EbolaViz2026
- 视图接口：所有视图统一 `init(dom, store, data)` → `{ render, resize, destroy }`
- 状态管理：B 提供 `createStore` + `reducer` + `action creators`，C 的视图中通过 `store.dispatch(action)` 触发状态变更，通过 `store.subscribe(render)` 响应状态更新
- 数据接口：B 提供 `filterCases()` / `summarizeBy*()` 等工具函数，A 的 CSV 数据经 build_real_data.py 生成 JSON 后由前端 fetch 加载
- 调试接口：`window.__store.getState()` / `window.__data`（B 在 main.js 中挂载）
